# 前端构建时生效，修改后需重新 npm run build

# 是否启用验证码登录（true=显示验证码输入与「获取验证码」按钮；不设或 false=仅邮箱登录）
# VITE_ENABLE_EMAIL_VERIFICATION=true

# 生产环境 API 地址（仅前后端分离部署时需要；同机托管时可不设，默认 /api）
# VITE_API_BASE_URL=https://你的后端域名/api
