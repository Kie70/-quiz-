# 服务端口（默认 5000，部署时由平台分配则可不填）
PORT=5000

# 生产环境（部署时设为 production）
# NODE_ENV=production

# 数据库（可选。不填则使用 server/data/quiz.db）
# DATABASE_PATH=/data/quiz.db

# 是否启用邮箱验证码登录（true=需验证码；不设或 false=仅邮箱即可登录，便于内测）
# ENABLE_EMAIL_VERIFICATION=true

# 前后端分离部署时填前端地址（同机托管可不填）
# FRONTEND_URL=https://你的前端域名

# JWT 密钥（生产环境请更换为随机长字符串）
JWT_SECRET=your-secret-key

# ---------- 邮件发送（SMTP）-----------
# 不配置时仅控制台打印“Email Sent to [...]”，不会真实发信。
# 配置后课前 5 分钟会向用户邮箱发送 Quiz 提醒。

# SMTP 服务器（示例：学校邮箱、SendGrid、QQ 企业邮箱等）
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# 发件人邮箱（部分服务要求与 SMTP_USER 一致）
SMTP_FROM=quiz@xjtlu.local

# 使用方式：复制本文件为 .env，填入真实 SMTP 信息后重启 server。
